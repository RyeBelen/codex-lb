## 1. Proxy Implementation

- [x] 1.1 Route valid Codex compaction triggers through the normal upstream Responses stream and delete obsolete synthetic bridge code.
- [x] 1.2 Replace synthetic-compaction regression coverage with an integration test that proves the trigger and upstream SSE are proxied unchanged.

## 2. Validation

- [x] 2.1 Run targeted proxy and request-contract tests plus focused lint checks.
- [x] 2.2 Validate the OpenSpec change and main specs strictly.
